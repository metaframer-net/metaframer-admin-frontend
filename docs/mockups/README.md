# Design mockups

Standalone, self-contained HTML mockups used to preview and pick interaction /
motion options **before** they are built into the app. Each file opens directly
in a browser (no build step, no dependencies).

These are throwaway decision aids, not shipped code — open one with:

```
open docs/mockups/<name>.html
```

Keep them here (under `docs/`) rather than as shareable artifacts.
